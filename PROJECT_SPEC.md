# 山岭隧道系统锚杆分区拉拔抽检与复验封存闭环

## 项目目标

建设一个面向山岭隧道一轮系统锚杆施工后质量抽检的 Go HTTP 后端。任务先锁定里程段、围岩等级、开挖循环、布置图修订摘要、候选孔位、材料批次、注浆摘要、抽样参数、仪器校准摘要、加载程序和判定阈值，再沿“确定抽样—孔位核对—设备租约—逐级加载与持荷—卸载回弹—不合格扩样—补强复验—双人复核—终局封存”推进。核心状态由按里程环号和拱部方位组织的空间分层样本树、单锚杆不可跳级的载荷状态机、带逻辑时钟的设备限时租约、同循环同材料批影响域以及原检和复验并存的证据版本链组成。采用事务型关系数据库持久化任务快照、样本、租约、证据、仪器调用、复核和终局凭据，并以操作号幂等记录、版本号和唯一约束支持崩溃恢复及并发仲裁。实现规模限定为约20至28个生产Go文件、2000至3000有效生产行且目标约2500行，分布在至少4个有实际职责的包中；公开测试不少于4个测试文件和12个独立用例。仓库需包含go.mod、数据库迁移、可编译服务入口，以及可构建linux/amd64和linux/arm64镜像的多阶段Dockerfile。

## 端到端业务流程

1. 创建待锁定任务，录入并规范化隧道里程段、围岩级别、开挖循环、布置图修订及摘要、孔位坐标、杆体与锚固剂批次、注浆记录摘要、抽样种子与分层配额、设备校准摘要、加载程序、停载阈值和任务代次；校验全部引用一致且摘要未陈旧后一次性锁定。
2. 从按里程、环号、方位、孔位排序的候选序列构建空间分层样本树，使用公开固定的伪随机算法逐层选样；任何重复归属、配额不足或规范化冲突都会使整个抽样事务失败，成功后任务进入孔位核对中。
3. 现场人员逐孔确认里程、环号、方位、坐标、材料批次和布置图归属；全部样本核对完成后，试验以原子事务为拉拔仪、泵站和位移计获取同一时间窗租约，随后进入加载试验中。
4. 每根锚杆按照锁定程序依次记录预载、各级加载、持荷读数和卸载回弹；每次读数验证任务代次、设备租约、前置级次、持荷时间与整数定点算术，仪器拒绝、断连或超时只生成确定的待重试调用。
5. 任一原检或扩样锚杆判为不合格时，按锁定的同循环、同杆体或锚固剂批次边界计算唯一影响域并进入扩样中；边界数据不完整时转入待复核，不允许猜测扩样范围。
6. 扩样仍不满足规则时进入待补强；补强记录封存后创建递增的新检验代次并进入复验中，复验复用锁定规则但生成独立证据链，旧代次迟到读数仅作为审计证据保存。
7. 原样本、扩样和当前复验全部闭合后进入待复核；两名不同且具备资格的人员分别签署，竞争请求通过数据库条件更新争夺唯一终局，最终只能成为已通过、质量隔离、已取消之一并封存不可变凭据。

## 核心组件与职责

1. 围岩支护与抽检规则目录：维护围岩等级、加载程序、分层配额、影响域规则、人员资格，以及布置图、注浆和校准摘要的有效版本；锁定后只引用不可变快照。
2. 锚杆检验任务聚合：管理任务代次、业务状态、锁定快照、操作号幂等、乐观版本和允许的状态转换，协调事务提交与重启恢复。
3. 空间样本及设备租约管理器：规范化孔位并构建空间分层样本树，执行确定抽样、重复归属校验，以及三类设备租约的原子获取、续租、到期和释放。
4. 分级加载与位移证据账簿：实施单锚杆载荷状态机、整数定点运算、持荷校验、仪器调用重试和按代次追加且不可改写的证据记录。
5. 扩样复验及终局仲裁器：计算唯一影响域，管理补强与复验代次隔离、双人复核、并发终局竞争和通过凭据或隔离记录封存。
6. Go HTTP API：提供版本化JSON接口、统一稳定错误码、规范请求校验、操作号幂等语义及按里程、环号、方位、孔位、载荷级排序的确定错误响应。

## 领域规则与不变量

1. 任务业务状态限定为待锁定、待抽样、孔位核对中、加载试验中、扩样中、待补强、复验中、待复核、已通过、质量隔离和已取消；状态转换由任务聚合集中校验，终局状态不可逆。
2. 锁定必须原子冻结里程段、围岩等级、开挖循环、布置图摘要、全部候选孔位、材料与锚固剂批次、注浆摘要、抽样种子、分层配额、加载程序、判定阈值、设备校准摘要及任务代次；里程或循环错配、引用版本陈旧时整体拒绝。
3. 孔位规范键由里程毫米值、环号、标准化方位和孔位号构成；一个锁定任务内不得重复，孔位只能属于一个空间层，排序依次使用里程、环号、方位序值和孔位号。
4. 确定抽样采用文档化的SplitMix64生成器：以锁定的无符号64位种子初始化，对每个已排序分层执行Fisher-Yates洗牌并取配额前缀；整数溢出按算法规定的模2^64运算，禁止依赖map遍历顺序。
5. 设备租约以设备编号、开始逻辑时刻和截止逻辑时刻表示半开区间；任意重叠区间内同一设备只能属于一个试验，拉拔仪、泵站、位移计必须在单事务中全部取得，续租和释放均检查持有者、代次与版本。
6. 锚杆载荷阶段严格为预载、锁定的第1至N级加载、各级持荷确认、卸载及回弹确认；只接受当前期望动作，重复或越级动作、错误设备、过期租约和错误代次均不得推进前缀。
7. 荷载、位移、回弹率和判定比值使用有明确定标因子的有符号64位整数；乘除前检查符号、除零和溢出，除法统一使用向最接近值取整且恰好一半时远离零，持荷秒数使用非负整数逻辑时间差。
8. 仪器调用以任务、锚杆、代次、载荷级、设备和调用序号唯一标识；拒绝、断连、超时记录为待重试，重试次数及下一逻辑时刻由固定退避表确定，失败调用不得生成合格级次或释放租约。
9. 不合格影响域仅包含锁定候选集中与失败点同开挖循环且共享杆体批次或锚固剂批次、并满足锁定方位及相邻环边界规则的孔位；计算结果规范排序并以摘要去重，边界资料缺失则不得扩样。
10. 补强完成后创建严格递增的新代次；每条证据保留所属代次和原检、扩样或复验类别，旧代次迟到证据可追加为隔离审计项，但不得更新当前载荷前缀、判定或任务状态。
11. 所有变更请求必须携带操作号；同一操作号与相同规范化内容重复提交返回原结果，不同内容返回稳定冲突码。失败事务不得残留部分样本、部分设备租约、部分加载级次或部分终局结论。
12. 只有当前代次全部样本闭合、影响域无遗漏、补强与复验要求满足，并由两名不同且资格有效的人员复核后才能通过；通过、质量隔离和取消共享唯一终局槽，数据库竞争只允许一个结果成功。

## 数据模型与持久化

1. RuleSnapshot：规则快照ID、围岩等级、抽样算法版本、分层配额、加载级定义、停载和合格阈值、影响域边界、有效期与内容摘要。
2. InspectionTask：任务ID、里程起止毫米、围岩等级、循环号、布置图修订与摘要、注浆摘要、抽样种子、当前代次、业务状态、乐观版本、锁定时间和终局槽。
3. CandidateHole：任务ID、规范里程、环号、方位序值、孔位号、坐标定点值、杆体批次、锚固剂批次及空间层键，并对任务内规范孔位和层归属设置唯一约束。
4. SampleNode：样本ID、父层节点、空间层键、孔位引用、样本类别、抽中顺序、来源失败点、影响域摘要、代次和闭合状态。
5. Device与CalibrationSnapshot：设备编号、类型、状态、校准版本、摘要和有效区间；锁定及读数均可追溯至校准快照。
6. DeviceLease：租约ID、设备编号、试验ID、任务代次、开始和截止逻辑时刻、租约版本及释放时刻；数据库约束配合事务检测时间窗重叠。
7. LoadEvidence：任务、样本、代次、阶段、载荷级、荷载定点值、位移定点值、持荷秒数、回弹定点值、判定比值、设备组合、证据序号、接收结果和内容摘要。
8. InstrumentCall：确定调用键、请求摘要、调用序号、结果类型、失败类别、重试次数、下一重试逻辑时刻及关联证据，失败记录可审计但不推进级次。
9. ReinforcementAndGeneration：影响域摘要、补强记录摘要、前代次、新代次、生成原因和生效逻辑时刻，用于约束复验代次单调递增。
10. ReviewAndVerdict：复核人、资格快照、签署摘要、终局类型、终局凭据编号、封存摘要与提交版本；复核人唯一且终局凭据对任务唯一。
11. OperationRecord：操作号、任务ID、接口动作、规范请求摘要、响应码、响应摘要和提交版本，用于跨重启的幂等重放。

## 公开接口

1. POST /v1/tasks：创建待锁定任务；POST /v1/tasks/{id}/lock：校验目录引用并原子冻结完整任务快照。
2. POST /v1/tasks/{id}/samples：执行确定分层抽样；GET /v1/tasks/{id}/sample-tree：返回按规范顺序组织的空间样本树。
3. POST /v1/tasks/{id}/holes/{sampleId}/verify：核对孔位；批量请求任一项失败时不提交任何核对结果。
4. POST /v1/tests/{testId}/leases、/renew、/release：原子获取设备组合、续租或释放；请求包含逻辑时刻、任务代次、期望版本和操作号。
5. POST /v1/tests/{testId}/readings：提交预载、分级载荷、持荷、卸载或回弹读数；GET /v1/tests/{testId}/evidence：按代次和载荷级查询不可变证据链。
6. POST /v1/instrument-calls/{callId}/retry：按确定调用键执行脚本化重试；公开测试通过进程内可控适配器注入拒绝、断连、超时和成功序列。
7. POST /v1/tasks/{id}/expansion：计算并提交影响域扩样；POST /v1/tasks/{id}/reinforcements：封存补强；POST /v1/tasks/{id}/retests：创建下一复验代次。
8. POST /v1/tasks/{id}/reviews与POST /v1/tasks/{id}/finalize：提交资格复核并竞争终局；GET /v1/tasks/{id}返回当前状态、版本和封存凭据。
9. 错误响应包含稳定code、operation_id、task_version及reasons数组；原因按里程、环号、方位、孔位、载荷级和错误码排序，不暴露数据库或设备适配器内部文本。

## 失败边界

1. 目录锁定事务覆盖任务快照及全部候选孔位；里程或循环错配、摘要陈旧、重复孔位、无效配额等任一错误会回滚全部写入。
2. 抽样事务同时写入分层节点、叶样本和任务状态；配额不足、分层偏差或摘要冲突不会留下部分样本树。
3. 租约事务以数据库锁、版本条件和重叠检查保护三类设备组合；竞争失败、进程崩溃或续租冲突不产生部分租约，过期租约由逻辑时钟判定而非后台时间竞态。
4. 加载事务只追加一个合法阶段证据并推进一个状态机前缀；算术异常、持荷不足、错误设备、仪器失败、操作号冲突或数据库错误均保持原前缀。
5. 仪器外部调用与业务证据通过持久化调用记录分隔；重启后依据调用键和状态恢复待重试队列，无法确认的调用不会被推定成功。
6. 扩样和复验事务以影响域摘要、任务版本和代次唯一约束防止重复计算及代次分叉；旧代次事件被隔离到审计链。
7. 终局事务原子校验样本闭合、两名合格复核人和空终局槽；并发通过、隔离与取消只允许一个提交，其余返回同一可重放的终局冲突。
8. 所有写接口在同一事务内写入OperationRecord；服务重启后仍能重放相同请求结果，并能识别同操作号不同内容的冲突。

## 验收标准

1. 锁定操作一次性冻结里程段、围岩等级、开挖循环、布置图摘要、全部候选孔位、材料批次、抽样种子、分层配额、加载程序、阈值、校准摘要和任务代次；错配或陈旧输入整体拒绝且无部分快照。
2. 系统对规范化孔位序列使用公开SplitMix64与Fisher-Yates规则分层抽样；相同输入产生逐字节相同的样本树，配额不足、重复孔位或重复归属使整个事务失败。
3. 拉拔仪、泵站和位移计在重叠逻辑时窗内最多归属一个试验，并发获取、续租和释放具备原子性；服务重启后租约所有权与截止时刻保持一致。
4. 每根锚杆仅能依序完成预载、各级加载和持荷、卸载及回弹；越级、重复、错误设备、过期租约、错误代次或持荷不足均返回稳定错误且不推进证据前缀。
5. 全部荷载、位移、回弹率、持荷秒数和判定比值采用受检整数定点运算，符号、舍入、除零及边界溢出行为有精确可复现结果。
6. 仪器拒绝、断连和超时只产生持久化待重试调用；调用对象、次数、固定退避逻辑时刻与脚本结果确定，失败不写合格级次、不提前释放租约，重启后可继续重试。
7. 任一不合格点按锁定规则得到唯一、规范排序的同循环同材料批影响域；补强后仅新代次复验可改变当前结论，旧代次迟到读数保留审计但不影响状态。
8. 原样本、扩样及当前复验闭合并由两名不同合格人员复核后，竞争成功请求生成唯一通过凭据；通过、质量隔离和取消至多一个终局，失败事务和幂等冲突不留下部分结论。

## 确定性测试场景

1. 使用虚构里程K12+340至K12+380、固定环号和孔位，断言合法锁定快照字段完整且重启后内容摘要不变。
2. 提交跨出里程段或错误循环候选孔位，断言锁定整体失败、原因规范排序且数据库无候选残留。
3. 使用公开种子和多方位配额两次抽样，精确断言样本ID顺序、空间树结构和结果摘要一致。
4. 构造同一规范孔位的重复坐标表示及配额不足层，断言分别返回稳定错误码并且零样本写入。
5. 用同步屏障让两个试验并发获取同一拉拔仪、泵站和位移计，断言仅一个完整设备组合成功且不存在拆分租约。
6. 覆盖相邻半开租约时窗、重叠续租、错误持有者释放和进程重启恢复，断言所有权与逻辑截止判断确定。
7. 依次提交预载、两级加载、持荷、卸载和回弹，逐步断言单锚杆合法载荷前缀及最终判定。
8. 分别提交越级、重复级次、错误设备、旧代次和持荷不足读数，断言证据计数及状态版本均不前进。
9. 以整数边界数据覆盖负数、除零、乘法溢出和恰好半数舍入，精确断言计算值或稳定算术错误码。
10. 注入仪器拒绝、断连、超时、成功脚本，断言调用序号、固定重试次数、下一逻辑时刻、租约保留和最终单次证据写入。
11. 令同循环且共享杆体批或锚固剂批的多个方位孔位围绕失败点分布，精确断言影响域成员、排序、去重和摘要。
12. 缺失相邻环边界资料时触发扩样，断言任务进入待复核且没有猜测生成的扩样样本。
13. 完成补强并创建新代次后提交旧代次迟到读数，断言其进入隔离审计链且当前复验前缀与结论不变。
14. 并发请求创建同一复验代次，断言唯一代次成功，另一请求返回代次冲突且没有分叉样本树。
15. 相同操作号和相同内容重复提交，断言响应完全重放；相同操作号携带不同内容时断言内容冲突且无业务写入。
16. 使用两名不同合格人员完成复核，并以同步屏障并发提交通过、质量隔离和取消，断言仅一个终局和一个封存记录。
17. 尝试由同一人员双重复核、资格过期人员复核及样本未闭合时终局，断言均被拒绝且终局槽为空。
18. 在抽样、设备组合获取、加载写入和终局事务的故障注入点模拟数据库失败，重启后断言不存在部分样本、部分租约、部分级次或部分结论。

## 组件追踪关系

1. 围岩支护与抽检规则目录对应锁定字段、陈旧摘要、人员资格、配额、加载阈值和影响域边界，主要落实验收1、2、7、8。
2. 锚杆检验任务聚合对应全部业务状态、任务代次、操作号幂等、事务回滚和恢复，贯穿验收1至8。
3. 空间样本及设备租约管理器对应空间分层样本树、公开确定抽样、孔位唯一归属和三设备并发租约，主要落实验收2、3。
4. 分级加载与位移证据账簿对应不可跳级载荷状态机、定点算术、仪器失败重试和版本化证据，主要落实验收4、5、6。
5. 扩样复验及终局仲裁器对应不合格影响域、补强、新代次复验、迟到证据隔离、双人复核和唯一终局，主要落实验收7、8。
6. Go HTTP API对应稳定错误码、确定原因排序、逻辑时钟测试入口、请求规范化和跨重启幂等重放，为全部验收提供可公开测试的接口。

## 独特性

本计划聚焦山岭隧道系统锚杆在单轮施工后的空间分区拉拔闭环，以“里程环号—拱部方位”样本树、三类试验设备组合租约、逐杆载荷前缀、同循环同材料批影响域和跨代次证据隔离形成领域特有的一致性模型，与既有放行、联检或一般批次验收项目在对象、状态推进和并发争议点上均不重复。
